## 2026-07-11

### DESIGN.md 重写（App Landing Page 风格）
- Project `safari-format-apps-site` (Astro): multi-app Landing Page / directory.
- 原设计照搬苹果产品页风格（hero 56px / tile h 40px / lead 28px），字号过大、密度过稀，不适合落地页场景。
- 重写 DESIGN.md：hero 48 / h2 32 / card-title 26 / lead 20 / body 16；间距 section 64 / hero 96；保留苹果设计语言 + getnoir.app 参考。

### Noir Landing Page 应用生成（画布还原）
- 通过 `open_design(fileId:702802631653996)` 成功连接 Ardot 画布「site」（文件名 site，权限 readwrite）。
- 画布为单张整页设计图：**Noir Safari 深色模式扩展的 Landing Page**（深紫 Hero → 四宫格 Features → CTA → Footer）。
- 截图分析后严格还原生成 `noir-landing.html`：
  - **配色**：Hero bg #2D1B4E, Body/Features/Footer bg #1C1C1E, Accent violet #A855F7
  - **布局**：flex 两栏 Hero（左文案+按钮 / 右 iPhone 叠层截图），Features 2×2 grid
  - **组件**：App Store 按钮（黑底圆角10px）、特性图标（紫底圆角方块）、Footer flex space-between
  - **响应式**：≤860px 单列，≤480px 缩小字号和手机框尺寸

### 用 Noir 风格重构 apps 站点真实代码
- 目标：把 Noir 设计稿的布局骨架 + 字体风格 + 深色配色语言落到真实 Astro 站点。
- **重写 `public/assets/styles.css`**（Noir 设计语言）：
  - 深色优先（默认 dark，`[data-theme=light]` 可切换）；tokens：canvas #0e0e11 / canvas-soft #16161b / surface #1c1c22 / accent 默认紫 #7c5cff（每个 App 用 `--accent` 覆盖）。
  - **全宽强调色 Hero 色带**：`.hero`/`.app-hero` 用 `width:100vw; margin-inline:calc(50%-50vw)` 满宽 + radial/linear accent 渐变；`html{overflow-x:hidden}` 防横向滚动。
  - **字体层级**：hero 48 / section 32 / card 26 / feature 19 / body 16 / eyebrow 13px（大写+字距）；app-hero 标题用 accent 着色。
  - 组件：药丸按钮、圆角卡片 hover 上浮、AppDock accent 瓷砖、平台 chip、安静页脚 + 链接 hover accent。
- **改 `[app].astro`**：feature-section 从"标题+列表"改为 `.section-head` + `.feature-grid`（2×2），每格 `.feature-chip`（accent 勾选图标）+ 文案。
- 构建通过（50 页），`npm run preview` 端口 4331（base `/apps`）验证首页与 app 详情页正常。
- 注意：站点 base = `/apps`，预览 URL 需带 `/apps/` 前缀。
